# Binary search algorithm

numbers = list(range(100))


def find(items, item):
  pass
  # determine the section of items we need to examine
  # go to the middle of the section
  # is it the item we're looking for?
  # If so, we're done!
  # If not, determine the new section, and repeat.


assert True == find(items, 75)
assert False == find(items, 101)



