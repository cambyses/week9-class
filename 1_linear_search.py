# Linear search algorithm

numbers = list(range(100))


def find(items, item):
  pass
  # start at the first item of the list
  # is it the item we're looking for?
  # If so, we're done!
  # If not, go to the next item, and repeat.



assert True == find(items, 75)
assert False == find(items, 101)



